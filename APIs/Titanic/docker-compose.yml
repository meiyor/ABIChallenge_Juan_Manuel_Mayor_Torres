version: "3.9"
services:
    frontend:
        build: .
        ports:
            - "8000:8000"
        depends_on:
            - backend
        volumes:
            - ./results/:/app/results/
            - ./mounts/postgres/:/var/lib/postgresql/16/main/
            - ./mounts/sockets/:/var/run/postgresql/

    backend:
        image: postgres
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: DataBase
            POSTGRES_DB: apidb
        ports:
            - "5433:5433"

